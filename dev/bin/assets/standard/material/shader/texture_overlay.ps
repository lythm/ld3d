#version 330

uniform sampler2D overlay_image;

out vec4 final_color;

void main()
{
	ivec2 size = textureSize(overlay_image, 0);

	vec2 uv;
	uv.x = gl_FragCoord.x / size.x;
	uv.y = gl_FragCoord.y / size.y;

	final_color = texture(overlay_image, uv);
}
