#version 330

out vec4 d;

void main()
{
//	float d = 1.0 / gl_FragCoord.w;

//	float d = 1.0 - (gl_FragCoord.z / gl_FragCoord.w);
	
//	o_color = vec4(d, 0, 0, 1);	

	d = vec4(gl_FragCoord.z, 0, 0, 0);
}
