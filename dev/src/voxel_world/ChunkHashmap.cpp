#include "voxel_world_pch.h"
#include "ChunkHashmap.h"

namespace ld3d
{
	namespace voxel
	{
		ChunkHashmap::ChunkHashmap(void)
		{

		}


		ChunkHashmap::~ChunkHashmap(void)
		{
		}
		void ChunkHashmap::AddChunk(const ChunkKey& key, ChunkPtr pChunk)
		{

		}
		ChunkPtr ChunkHashmap::FindChunk(const ChunkKey& key)
		{
			return nullptr;
		}
		ChunkPtr ChunkHashmap::RemoveChunk(const ChunkKey& key)
		{
			return nullptr;
		}
		bool ChunkHashmap::Initialize()
		{
			return true;
		}
		void ChunkHashmap::Release()
		{

		}
	}
}
